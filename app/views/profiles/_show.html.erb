<div class="user-info">
  <h3 id='name'><%= "#{@user.first_name} #{@user.last_name}" %></h3>
  <h4 id='location'><%= @user.location %></h4>
  <% if @can_edit %>
    <%= link_to 'Edit', edit_profile_path(id: @user.id), remote: true %>
  <% end %>
</div>

<p><%= @user.email %></p>

  <table>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  <% @user.groups.each do |group| %>
    <tr>
      <td> <%= group.name %></td>
      <td> <%= group.description %></td>
    </tr>
  <% end %>
  </table>
