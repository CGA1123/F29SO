<%= form_for @user, url: profile_path(id: @user.id), method: :patch, remote: true do |f| %>
<% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :first_name, placeholder: 'Joe' %>
  <%= f.text_field :last_name, placeholder: 'Bloggs' %>
  <br />
  <%= f.text_field :location, placeholder: 'Edinburgh' %>
  <%= f.submit 'Update' %>
<% end %>

<%= link_to 'Cancel', profile_path(id: @user.id) %>
