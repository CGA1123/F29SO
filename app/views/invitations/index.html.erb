<h3>Invitation Index:</h3>
<ul>
  <% @invitations.each do |invite| %>
  <li>Invite For: <%= invite.email %> <%= link_to 'Delete', invitations_path(invitation_id: invite.id), method: :delete %></li>
  <% end %>
</ul>
