<p>Results</p>
<% if skills.nil? %>
  <div>
    No skills found.
  </div>
<% else %>
  <% skills.each do |skill| %>
    <div skill-name="<%= skill.name %>">
      <%= button_to "#{skill.name}", user_skills_path(skill_id: skill.id), method: :post %>
    </div>
  <% end %>
<% end %>
