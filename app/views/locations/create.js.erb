<% if @location.persisted? %>
    $('#db-locations').prepend('<%= j render 'locations/location', location: @location %>')
    $('#locations-counter').html('Locations (<%= Location.count %>)')
<% end %>
